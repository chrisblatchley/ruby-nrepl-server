# frozen_string_literal: true

RSpec.describe Nrepl do
  it "has a version number" do
    expect(Nrepl::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
